import React, { useEffect, useState } from 'react'
import Avatar from './Avatar'
import { api } from '../services/api'

export default function SidebarRight(){
  // Right sidebar intentionally empty per user request
  return null
}
